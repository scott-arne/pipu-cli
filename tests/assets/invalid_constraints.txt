# File with some invalid lines that should be ignored
requests==2.31.0

# Invalid lines (malformed)
invalid-line-without-version
==1.0.0
>=
package name with spaces==1.0.0

# Valid line after invalid ones
numpy>=1.20.0